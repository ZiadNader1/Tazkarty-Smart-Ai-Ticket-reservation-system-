import { LazyLoadImage } from './lazy-load-image';

describe('LazyLoadImage', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadImage();
    expect(directive).toBeTruthy();
  });
});
